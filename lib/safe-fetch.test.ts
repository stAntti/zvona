import {describe,expect,it} from 'vitest'
import {isPrivateAddress,validatePublicUrl} from './safe-fetch'
describe('safe public research fetcher',()=>{it('blocks private and reserved IPv4 ranges',()=>{expect(isPrivateAddress('127.0.0.1')).toBe(true);expect(isPrivateAddress('10.1.2.3')).toBe(true);expect(isPrivateAddress('192.168.1.2')).toBe(true);expect(isPrivateAddress('100.64.0.1')).toBe(true);expect(isPrivateAddress('100.127.255.254')).toBe(true);expect(isPrivateAddress('100.128.0.1')).toBe(false);expect(isPrivateAddress('8.8.8.8')).toBe(false)});it('blocks IPv4-mapped IPv6 loopback and private targets',()=>{expect(isPrivateAddress('::ffff:127.0.0.1')).toBe(true);expect(isPrivateAddress('::ffff:7f00:1')).toBe(true);expect(isPrivateAddress('::ffff:192.168.1.2')).toBe(true);expect(isPrivateAddress('::ffff:8.8.8.8')).toBe(false)});it('blocks loopback hostnames after DNS resolution',async()=>{await expect(validatePublicUrl('http://localhost/admin')).rejects.toThrow()});it('rejects non-http protocols',async()=>{await expect(validatePublicUrl('file:///etc/passwd')).rejects.toThrow('HTTP')})})
